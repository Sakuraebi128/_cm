這段程式碼會開啟一個視窗，顯示雙擺的即時運動，並繪製出末端擺錘的動態軌跡，讓你直觀地感受混沌之美。</br>
如何執行與觀察：
執行環境： 請確保你的電腦安裝了 Python，並執行過 pip install numpy matplotlib scipy。

觀察軌跡： 程式碼中的 trace 會畫出第二個擺錘的歷史路徑。

實驗「混沌」： * 你可以修改 initial_state 中的 np.radians(120)，改為 120.001。

再次執行後，你會發現雖然只差了 0.001 度，但幾秒後的動作會跟原本完全不同。
